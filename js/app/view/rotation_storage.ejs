<ul>
<% can.each(storage, function(rotation) { %>
<li class="rotation_st" <%= (el) -> can.data(el, 'rotation', rotation) %>><img src="images/<%= rotation.spec.icon %>" /><span class="rotation_name"><%= rotation.nick %></span><a class="destroy">x</a></li>
<% }) %>
</ul>
